import { GenerateCertificate } from "@/components/generate/generate-certificate";

const Generate = () => {
  return (
    <div>
      <GenerateCertificate />
    </div>
  );
};

export default Generate;
